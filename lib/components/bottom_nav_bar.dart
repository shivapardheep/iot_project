import 'package:flutter/material.dart';

class BottomNavBar extends StatelessWidget {
  const BottomNavBar({
    Key? key,
    required this.destination,
  }) : super(key: key);

  final GestureTapCallback destination;
  @override
  Widget build(BuildContext context) {
   
    return Container();
  }
}
